server {

    listen 10.0.2.15:80;
    server_name _;
    access_log <%= @service_root_folder %>/logs/default.access.log;

    server_name_in_redirect off;

    root  <%= @docroot %>;
    index index.html;
    charset utf-8;

    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
        
    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    location ~ /\.ht {
        deny  all;
    }
}
